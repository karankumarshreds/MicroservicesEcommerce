Used to fake create an instance of something for the testing purposes because we cannot have all the dev environments instances in the Jest testing env.

*Make the same name as the file which you're tying to fake*.